query Posts($input: PaginationInput!) {
  posts(input: $input) {
    posts {
      id
      title
      textSnippet
      text
      voteStatus
      points
      createdAt
      updatedAt
      creator {
        id
        username
      }
      commentCount
    }
    hasMore
  }
}
