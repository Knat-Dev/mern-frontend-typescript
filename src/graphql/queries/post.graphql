query Post($id: String!) {
  post(id: $id) {
    id
    title
    text
    textSnippet
    commentCount
    voteStatus
    points
    createdAt
    updatedAt
    creator {
      id
      username
    }
  }
}
